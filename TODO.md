# TODO

- [x] Replace Streamlit’s direct `BookingAgent` invocation in `app_ai.py` with a call to the existing OpenClaw tool `book_ev_slot`.
- [ ] Ensure returned JSON shape remains compatible with the Streamlit UI (use `final_outcome` semantics).
- [ ] Keep changes minimal and confined to integration layer.
- [ ] Verify with a sample request in Streamlit.

